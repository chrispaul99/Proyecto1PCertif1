@model IEnumerable<BEUProyecto.Comerciante>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.baseLegal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persona.nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.baseLegal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idComerciante }) |
            @Html.ActionLink("Details", "Details", new { id=item.idComerciante }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idComerciante })
        </td>
    </tr>
}

</table>
